# Agenda

## Configuracao do banco

1. Copie `config/database.example.php` para `config/database.php`.
2. Ajuste `host`, `dbname`, `user` e `password` conforme seu MySQL local.
3. O arquivo `config/database.php` fica fora do Git para nao versionar credenciais.
